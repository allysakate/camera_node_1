# Template for camera_secrets.yaml — copy to camera_secrets.yaml and fill in.
# camera_secrets.yaml is gitignored. Deep-merged on top of camera_params.yaml at load time.
# Override path: set CAMERA_NODE_SECRETS env var to point at a custom copy.

spb_bridge:
  local:
    host:     localhost
    username: ""
    password: ""

  hivemq:
    host:     ""       # e.g. abc123.s1.eu.hivemq.cloud
    username: ""
    password: ""
